'use client';
import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { useState } from 'react'; import AuthFrame from '@/components/auth/AuthFrame'; import { useRegister } from '@/hooks/useAuth';
export default function RegisterPage() { const router = useRouter(); const mutation = useRegister(); const [form, setForm] = useState({ name: '', email: '', phone: '', password: '' }); return <AuthFrame eyebrow="Begin your collection" title="Create an account"><form data-testid="register-form" className="mt-8 space-y-4" onSubmit={(event) => { event.preventDefault(); mutation.mutate(form, { onSuccess: () => router.push('/account') }); }}>{[['name','Full name','text'],['email','Email','email'],['phone','Phone','tel'],['password','Password','password']].map(([key,label,type]) => <label key={key} className="block text-sm text-ink">{label}<input data-testid={`register-${key}`} required={key !== 'phone'} type={type} value={form[key as keyof typeof form]} onChange={(e) => setForm({ ...form, [key]: e.target.value })} className="mt-2 h-12 w-full rounded border border-border bg-bg px-4 outline-none focus:border-gold" /></label>)}{mutation.isError ? <p data-testid="register-error" className="text-sm text-danger">{mutation.error.message}</p> : null}<button data-testid="register-submit" disabled={mutation.isPending} className="h-12 w-full rounded-full bg-ink text-xs font-semibold uppercase tracking-wider2 text-bg hover:bg-gold">Create account</button><p className="text-center text-sm text-ink-2">Already a member? <Link data-testid="register-login-link" href="/auth/login" className="text-gold">Sign in</Link></p></form></AuthFrame>; }