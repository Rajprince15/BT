'use client';
import Link from 'next/link'; import { useState } from 'react'; import AuthFrame from '@/components/auth/AuthFrame'; import authService from '@/services/auth.service';
export default function ForgotPasswordPage() { const [email, setEmail] = useState(''); const [sent, setSent] = useState(false); return <AuthFrame eyebrow="Account care" title="Reset your password"><form data-testid="forgot-password-form" className="mt-8 space-y-5" onSubmit={async (event) => { event.preventDefault(); await authService.forgotPassword(email); setSent(true); }}>{sent ? <p data-testid="forgot-password-success" className="rounded border border-success/30 bg-success/10 p-4 text-sm text-success">If the email is registered, a reset link has been sent.</p> : <><label className="block text-sm text-ink">Email<input data-testid="forgot-password-email" required type="email" value={email} onChange={(e) => setEmail(e.target.value)} className="mt-2 h-12 w-full rounded border border-border bg-bg px-4 focus:border-gold" /></label><button data-testid="forgot-password-submit" className="h-12 w-full rounded-full bg-ink text-xs font-semibold uppercase tracking-wider2 text-bg hover:bg-gold">Send reset link</button></>}<Link data-testid="forgot-password-back" href="/auth/login" className="block text-center text-sm text-ink-2 hover:text-gold">Back to sign in</Link></form></AuthFrame>; }