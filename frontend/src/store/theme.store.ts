'use client';

/**
 * Theme state is owned by `providers/ThemeProvider.tsx` (React context + localStorage 'bt_theme').
 * This file is reserved for any future Zustand-based theme experiments — kept intentionally empty.
 * Do NOT import from here; use `useContext(ThemeContext)` or the `useTheme` hook.
 */
export {};
