'use client';
import Link from 'next/link'; import { usePathname } from 'next/navigation';
const links = [['/account','Overview'],['/account/profile','Profile'],['/account/addresses','Addresses'],['/account/orders','Orders'],['/account/wishlist','Wishlist'],['/account/reviews','Reviews'],['/account/notifications','Notifications']];
export default function AccountShell({ children }: { children: React.ReactNode }) { const path = usePathname(); return <main data-testid="account-shell" className="bg-bg"><div className="mx-auto grid max-w-7xl gap-10 px-6 py-10 lg:grid-cols-[220px_1fr]"><aside><p className="text-xs font-semibold uppercase tracking-wider2 text-gold">Your atelier</p><h1 className="mt-2 font-serif text-3xl text-ink">Account</h1><nav data-testid="account-navigation" className="mt-8 grid gap-1">{links.map(([href,label]) => <Link key={href} data-testid={`account-nav-${label.toLowerCase()}`} href={href} className={`rounded px-3 py-2 text-sm ${path === href ? 'bg-gold-soft/40 text-ink' : 'text-ink-2 hover:text-gold'}`}>{label}</Link>)}</nav></aside><section>{children}</section></div></main>; }