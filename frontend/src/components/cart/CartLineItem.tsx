'use client';
import Image from 'next/image'; import { Minus, Plus, Trash2 } from 'lucide-react'; import type { CartItem } from '@/types/CartItem';
export default function CartLineItem({ item, onQuantity, onRemove }: { item: CartItem; onQuantity: (quantity: number) => void; onRemove: () => void }) { return <article data-testid={`cart-line-${item.id}`} className="flex gap-4 border-b border-border py-5"><div className="relative size-24 shrink-0 overflow-hidden rounded bg-surface-2"><Image src={item.imageUrl || '/images/editorial/premium-cotton.svg'} alt={item.productName} fill sizes="96px" className="object-cover" /></div><div className="min-w-0 flex-1"><h2 data-testid={`cart-item-name-${item.id}`} className="font-serif text-lg text-ink">{item.productName}</h2><p className="mt-1 text-xs uppercase tracking-wider2 text-ink-2">{item.productSku}</p><p className="mt-2 text-sm text-ink">₹{item.price.toLocaleString('en-IN')}</p><div className="mt-3 flex items-center justify-between"><div className="flex h-9 items-center rounded-full border border-border"><button type="button" data-testid={`cart-decrease-${item.id}`} aria-label="Decrease quantity" onClick={() => onQuantity(item.quantity - 1)} className="inline-flex size-9 items-center justify-center"><Minus className="size-3" /></button><span data-testid={`cart-quantity-${item.id}`} className="w-7 text-center text-sm">{item.quantity}</span><button type="button" data-testid={`cart-increase-${item.id}`} aria-label="Increase quantity" onClick={() => onQuantity(item.quantity + 1)} className="inline-flex size-9 items-center justify-center"><Plus className="size-3" /></button></div><button type="button" data-testid={`cart-remove-${item.id}`} onClick={onRemove} className="inline-flex items-center gap-1 text-xs uppercase tracking-wider2 text-danger"><Trash2 className="size-3" /> Remove</button></div></div></article>; }