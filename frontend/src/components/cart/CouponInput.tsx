'use client';
import { useState } from 'react'; import { CheckCircle2 } from 'lucide-react'; import { toast } from 'sonner';
export default function CouponInput() { const [code, setCode] = useState(''); return <form data-testid="coupon-form" className="mt-8 max-w-md" onSubmit={(event) => { event.preventDefault(); if (!code.trim()) { toast.info('Enter a code to apply it.'); return; } toast.info('This offer will be checked at checkout.'); }}><div className="flex"><input data-testid="coupon-input" value={code} onChange={(e) => setCode(e.target.value)} placeholder="Have a coupon code?" className="h-11 min-w-0 flex-1 rounded-l-full border border-border bg-bg px-5 text-sm text-ink outline-none focus:border-brand focus:ring-2 focus:ring-brand/20" /><button data-testid="coupon-apply-btn" type="submit" className="h-11 rounded-r-full bg-ink px-5 text-[11px] font-semibold uppercase tracking-[.16em] text-bg transition-colors hover:bg-brand">Apply</button></div>{code ? <p data-testid="coupon-status" className="mt-2 flex items-center gap-1 text-xs text-success"><CheckCircle2 className="size-3" /> Code ready to verify at checkout</p> : null}</form>; }