import Link from 'next/link'; import type { Cart } from '@/types/Cart';
export default function CartSummary({ cart }: { cart: Cart }) { return <aside data-testid="cart-summary" className="rounded-xl border border-border bg-surface p-6"><h2 className="font-serif text-2xl text-ink">Your total</h2><dl className="mt-6 space-y-3 text-sm text-ink-2"><div className="flex justify-between"><dt>Subtotal</dt><dd>₹{cart.subtotal.toLocaleString('en-IN')}</dd></div><div className="flex justify-between"><dt>Delivery</dt><dd>₹{cart.shipping.toLocaleString('en-IN')}</dd></div><div className="flex justify-between"><dt>Tax</dt><dd>₹{cart.tax.toLocaleString('en-IN')}</dd></div><div className="flex justify-between border-t border-border pt-4 text-base font-semibold text-ink"><dt>Total</dt><dd data-testid="cart-total">₹{cart.total.toLocaleString('en-IN')}</dd></div></dl><Link data-testid="proceed-checkout" href={cart.items.length ? '/checkout' : '/shop'} className="mt-7 flex h-12 items-center justify-center rounded-full bg-ink text-xs font-semibold uppercase tracking-wider2 text-bg hover:bg-gold">{cart.items.length ? 'Proceed to checkout' : 'Discover the atelier'}</Link></aside>; }