import Link from 'next/link';
import { RefreshCcw, ShieldCheck, Truck } from 'lucide-react';
import type { Cart } from '@/types/Cart';

export default function CartSummary({ cart }: { cart: Cart }) { return <aside data-testid="cart-summary" className="h-fit border-t border-border pt-6 lg:sticky lg:top-28"><p className="text-[10px] uppercase tracking-[0.24em] text-gold-2">Order summary</p><dl className="mt-7 space-y-4 text-sm text-ink-2"><div className="flex justify-between"><dt>Subtotal</dt><dd data-testid="cart-subtotal" className="text-ink">₹{cart.subtotal.toLocaleString('en-IN')}</dd></div><div className="flex justify-between"><dt>Shipping</dt><dd data-testid="cart-shipping" className="text-ink">₹{cart.shipping.toLocaleString('en-IN')}</dd></div><div className="flex justify-between"><dt>Tax</dt><dd data-testid="cart-tax" className="text-ink">₹{cart.tax.toLocaleString('en-IN')}</dd></div><div className="flex justify-between border-t border-border pt-5 text-ink"><dt className="font-serif text-2xl">Total</dt><dd data-testid="cart-total" className="font-serif text-2xl">₹{cart.total.toLocaleString('en-IN')}</dd></div></dl><Link data-testid="cart-checkout-cta" href={cart.items.length ? '/checkout' : '/shop'} className="mt-8 flex h-12 items-center justify-center bg-ink text-[10px] uppercase tracking-[0.2em] text-bg hover:bg-gold-2">{cart.items.length ? 'Proceed to checkout' : 'Explore catalogue'}</Link><div className="mt-7 grid gap-3 border-t border-border pt-6 text-xs text-ink-2"><p className="flex items-center gap-3"><ShieldCheck size={15} className="text-gold-2" />Secure order details</p><p className="flex items-center gap-3"><Truck size={15} className="text-gold-2" />Carefully packed delivery</p><p className="flex items-center gap-3"><RefreshCcw size={15} className="text-gold-2" />Support when you need it</p></div></aside>; }