'use client';

import { CheckCircle2, Circle } from 'lucide-react';
import type { Address } from '@/types/Address';

export default function AddressStep({ addresses, selected, onSelect }: { addresses: Address[]; selected?: number; onSelect: (id: number) => void }) {
  return <section data-testid="checkout-address-step" className="rounded-lg border border-border bg-surface p-6 sm:p-8 lg:p-10"><p className="text-[11px] font-semibold uppercase tracking-[.22em] text-brand">Step 1</p><h2 className="mt-3 font-serif text-3xl text-ink">Where should we deliver?</h2><p className="mt-2 text-sm text-ink-2">Choose the address your order should arrive at.</p><div className="mt-7 grid gap-3">{addresses.map((address) => { const active = selected === address.id; return <button type="button" key={address.id} data-testid={`checkout-address-${address.id}`} aria-pressed={active} onClick={() => onSelect(address.id)} className={`flex min-h-24 items-start gap-4 rounded-lg border p-5 text-left text-sm leading-6 transition-colors focus-visible:ring-2 focus-visible:ring-gold ${active ? 'border-brand bg-brand-soft' : 'border-border bg-bg hover:border-brand/50'}`}><span className="mt-0.5 shrink-0">{active ? <CheckCircle2 data-testid={`checkout-address-selected-${address.id}`} className="size-5 text-brand" /> : <Circle className="size-5 text-ink-2" />}</span><span className="flex-1"><strong data-testid={`checkout-address-name-${address.id}`} className="text-ink">{address.fullName}</strong>{active ? <span data-testid={`checkout-address-label-${address.id}`} className="ml-3 rounded-full bg-brand px-2 py-1 text-[10px] font-bold uppercase tracking-[.14em] text-brand-ink">Selected</span> : null}<span className="mt-1 block text-ink-2">{address.addressLine1}, {address.city}, {address.state} · {address.pincode}</span></span></button>; })}</div></section>;
}