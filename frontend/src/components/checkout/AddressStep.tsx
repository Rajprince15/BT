'use client';

import { CheckCircle2, Circle } from 'lucide-react';
import type { Address } from '@/types/Address';

export default function AddressStep({ addresses, selected, onSelect }: { addresses: Address[]; selected?: number; onSelect: (id: number) => void }) {
  return <section data-testid="checkout-address-step"><h2 className="font-serif text-2xl text-ink">Where should we deliver?</h2><div className="mt-5 grid gap-3">{addresses.map((address) => { const active = selected === address.id; return <button type="button" key={address.id} data-testid={`checkout-address-${address.id}`} aria-pressed={active} onClick={() => onSelect(address.id)} className={`flex items-start gap-4 rounded-xl border-2 p-5 text-left text-sm leading-6 transition-all ${active ? 'border-gold bg-ink text-bg shadow-luxe' : 'border-border bg-surface text-ink hover:border-gold/60'}`}><span className="mt-0.5 shrink-0">{active ? <CheckCircle2 data-testid={`checkout-address-selected-${address.id}`} className="size-5 text-gold" /> : <Circle className="size-5 text-ink-2" />}</span><span className="flex-1"><strong className={active ? 'text-bg' : 'text-ink'}>{address.fullName}</strong>{active ? <span data-testid={`checkout-address-label-${address.id}`} className="ml-3 rounded-full bg-gold px-2 py-1 text-[10px] font-bold uppercase tracking-wider2 text-ink">Selected</span> : null}<span className={`block ${active ? 'text-bg/70' : 'text-ink-2'}`}>{address.addressLine1}, {address.city}, {address.state} · {address.pincode}</span></span></button>; })}</div></section>;
}