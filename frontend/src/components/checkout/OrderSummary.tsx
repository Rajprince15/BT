import type { Cart } from '@/types/Cart';
export default function OrderSummary({ cart }: { cart: Cart }) { return <aside data-testid="checkout-order-summary" className="h-fit rounded-lg border border-border bg-surface p-6 sm:p-8 lg:sticky lg:top-24"><p className="text-[11px] font-semibold uppercase tracking-[.22em] text-brand">Your order</p><h2 className="mt-3 font-serif text-2xl text-ink">Order summary</h2><div className="mt-6 space-y-4 text-sm text-ink-2">{cart.items.map((item) => <div key={item.id} className="flex justify-between gap-4"><span>{item.productName} <span className="text-ink-2">× {item.quantity}</span></span><span className="shrink-0 text-ink">₹{(item.price * item.quantity).toLocaleString('en-IN')}</span></div>)}<div className="flex justify-between border-t border-border pt-5 text-ink"><span className="font-serif text-2xl">Total</span><span data-testid="checkout-total" className="font-serif text-2xl">₹{cart.total.toLocaleString('en-IN')}</span></div></div></aside>; }