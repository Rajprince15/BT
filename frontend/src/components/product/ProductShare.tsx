'use client';

import { useMemo, useState } from 'react';
import { Copy, Mail, MessageCircleMore, Share2 } from 'lucide-react';
import { toast } from 'sonner';
import env from '@/lib/env';
import type { Product } from '@/types/Product';

export default function ProductShare({ product }: { product: Product }) { const [copied, setCopied] = useState(false); const url = useMemo(() => `${env.NEXT_PUBLIC_APP_URL}/product/${product.slug}`, [product.slug]); const text = `${product.name} · Bhavita Textiles`; const copy = async () => { try { await navigator.clipboard.writeText(url); setCopied(true); toast.success('Link copied'); window.setTimeout(() => setCopied(false), 1800); } catch { toast.error('Copy failed. Please try again.'); } }; return <div data-testid="product-share-actions" className="mt-8 border-t border-border pt-6"><div className="flex items-center gap-2 text-[10px] uppercase tracking-[0.2em] text-gold-2"><Share2 size={15} />Share this piece</div><div className="mt-4 flex flex-wrap gap-5"><a data-testid="product-share-whatsapp" href={`https://wa.me/?text=${encodeURIComponent(`${text} ${url}`)}`} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 border-b border-border pb-2 text-[10px] uppercase tracking-[0.16em] text-ink hover:border-gold-2 hover:text-gold-2"><MessageCircleMore size={14} />WhatsApp</a><button type="button" data-testid="product-share-copy" onClick={copy} className="inline-flex items-center gap-2 border-b border-border pb-2 text-[10px] uppercase tracking-[0.16em] text-ink hover:border-gold-2 hover:text-gold-2"><Copy size={14} />{copied ? 'Copied' : 'Copy link'}</button><a data-testid="product-share-email" href={`mailto:?subject=${encodeURIComponent(text)}&body=${encodeURIComponent(url)}`} className="inline-flex items-center gap-2 border-b border-border pb-2 text-[10px] uppercase tracking-[0.16em] text-ink hover:border-gold-2 hover:text-gold-2"><Mail size={14} />Email</a></div></div>; }